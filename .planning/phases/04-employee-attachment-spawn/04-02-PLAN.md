---
phase: 04-employee-attachment-spawn
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/main/java/com/cxmxrgo/secondshift/employee/EmployeeNames.java
  - src/main/java/com/cxmxrgo/secondshift/employee/EmployeeManager.java
  - src/main/java/com/cxmxrgo/secondshift/gametest/EmployeeGameTests.java
autonomous: true
requirements: [EMP-01, EMP-02, EMP-08, EMP-09]

must_haves:
  truths:
    - "Calling EmployeeManager.bind spawns a real minecraft:villager positioned above the altar, with one of 3 fixed professions rolled at random"
    - "The spawned villager has villagerXp >= 1 so vanilla never resets its profession (EMP-02)"
    - "The spawned villager's name is green and always-visible (EMP-08)"
    - "The spawned villager carries a populated EmployeeData attachment (EMP-01)"
    - "A wild villager spawned in the same GameTest world is completely untouched by the bind — no attachment, no name, no XP change (EMP-09)"
  artifacts:
    - path: "src/main/java/com/cxmxrgo/secondshift/employee/EmployeeManager.java"
      provides: "static Villager bind(ServerLevel, BlockPos) — the phase's core, permanent infra"
    - path: "src/main/java/com/cxmxrgo/secondshift/employee/EmployeeNames.java"
      provides: "themed default-name pool + random picker (D-01)"
    - path: "src/main/java/com/cxmxrgo/secondshift/gametest/EmployeeGameTests.java"
      provides: "GameTest coverage for EMP-01/EMP-02/EMP-08/EMP-09 spawn correctness"
  key_links:
    - from: "EmployeeManager.bind"
      to: "villager.setData(ModAttachments.EMPLOYEE, new EmployeeData(...))"
      via: "attachment write, AFTER setOffers (last), per Pattern 2 ordering"
      pattern: "setData\\(ModAttachments\\.EMPLOYEE"
    - from: "EmployeeManager.bind"
      to: "villager.setVillagerData(...).setProfession(profession)"
      via: "profession set BEFORE setOffers (Pitfall 4 — setVillagerData nulls offers on profession change)"
      pattern: "setVillagerData"
---

<objective>
Implement `EmployeeManager.bind(ServerLevel, BlockPos)` — the phase's one genuinely new piece of
business logic — and prove it correct with GameTests covering all four phase requirements
(EMP-01, EMP-02, EMP-08, EMP-09).

Purpose: This is the real, permanent infrastructure the roadmap phase exists to deliver. It is
called by the (throwaway) Confirm-button network handler in Plan 04-03, but `bind(...)` itself is
not throwaway — it is the method every later phase's re-bind/respawn logic will also need to match.
Output: A working spawn path from "altar position" to "persistent named employee villager in the
world", verified by automated GameTests for everything GameTest can observe in one tick.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-employee-attachment-spawn/04-CONTEXT.md
@.planning/phases/04-employee-attachment-spawn/04-RESEARCH.md
@.planning/phases/04-employee-attachment-spawn/04-PATTERNS.md
@.planning/phases/04-employee-attachment-spawn/04-01-SUMMARY.md

<interfaces>
<!-- EmployeeData (Plan 04-01) — the attachment write target. Field order:
     (int version, String name, ResourceLocation profession, int tier, MerchantOffers offers). -->
<!-- ModAttachments.EMPLOYEE (Plan 04-01) — the AttachmentType<EmployeeData> holder. Access via
     ModAttachments.EMPLOYEE.get(). -->

From 04-RESEARCH.md "Code Examples" — authoritative spawn ordering (do not reorder):
```java
Villager v = EntityType.VILLAGER.create(level);
v.moveTo(pos.above().getX() + 0.5, pos.above().getY(), pos.above().getZ() + 0.5, 0f, 0f);
v.setVillagerData(v.getVillagerData().setProfession(profession).setLevel(1)); // FIRST
v.setVillagerXp(1); // or a value within the tier-1 XP band — see EMP-02
v.setOffers(defaultTier1Offers);                                              // LAST
v.setCustomName(Component.literal(name).withStyle(ChatFormatting.GREEN));
v.setCustomNameVisible(true);
v.setData(ModAttachments.EMPLOYEE, new EmployeeData(name, professionId, 1, defaultTier1Offers, 1));
level.addFreshEntity(v);
```
Note: the constructor argument order above is illustrative prose from RESEARCH.md; the actual
`EmployeeData` constructor order (Plan 04-01) is `(version, name, profession, tier, offers)` —
use the real record's declared order, not this snippet's argument order.

From 04-RESEARCH.md "Code Examples" — default tier-1 offer materialization:
```java
static MerchantOffers rollDefaultTier1Offers(Villager forEntity, VillagerProfession profession) {
    Int2ObjectMap<VillagerTrades.ItemListing[]> byTier = VillagerTrades.TRADES.get(profession);
    VillagerTrades.ItemListing[] tier1 = (byTier != null) ? byTier.get(1) : null;
    MerchantOffers offers = new MerchantOffers();
    if (tier1 != null) {
        RandomSource random = forEntity.getRandom();
        for (VillagerTrades.ItemListing listing : tier1) {
            if (offers.size() >= 2) break;                 // vanilla: max 2 per tier
            MerchantOffer offer = listing.getOffer(forEntity, random); // may be null
            if (offer != null) offers.add(offer);
        }
    }
    return offers; // may legitimately be empty — do not crash
}
```

From trade/ProfessionResolver.java — static-utility-class shape to copy for both new classes:
```java
public final class ProfessionResolver {
    private ProfessionResolver() {}
    public static Optional<VillagerProfession> fromAbove(Level level, BlockPos altarPos) { ... }
}
```

From gametest/HarvesterGameTests.java — GameTest authoring convention to copy exactly:
`@GameTestHolder(SecondShift.MODID)`, `@PrefixGameTestTemplate(false)`, `@GameTest(template = "empty")`,
`helper.spawn(EntityType.X, SPAWN)`, `helper.assertTrue(cond, "message")`, `helper.succeed()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EmployeeNames themed default-name pool</name>
  <files>src/main/java/com/cxmxrgo/secondshift/employee/EmployeeNames.java</files>
  <read_first>
    - src/main/java/com/cxmxrgo/secondshift/trade/ProfessionResolver.java (static-utility-class shape)
    - .planning/phases/04-employee-attachment-spawn/04-CONTEXT.md (D-01 — themed pool recommended over counter)
  </read_first>
  <action>
    Create `employee/EmployeeNames.java`: a `public final class` with private no-arg constructor,
    one `private static final List&lt;String&gt; POOL` field containing 12-20 "Second Shift" HR-themed
    office-worker names (matching the existing en_us.json flavor — payroll/paperwork/position
    language, e.g. "Gary from Accounts", "Doreen the Intern", "Bartholomew Ledger" — Claude's
    discretion per D-01/A2), and one public static method
    `public static String pickRandom(RandomSource random)` returning
    `POOL.get(random.nextInt(POOL.size()))`. No persisted counter state (A2 — a themed pool needs no
    world-scoped state, unlike an "Employee #N" counter).
  </action>
  <acceptance_criteria>
    - `EmployeeNames.POOL` has at least 12 distinct, non-blank entries
    - `EmployeeNames.pickRandom(RandomSource)` never returns null or an empty string for any seed
    - No field or method in this class holds mutable state across calls (pure pool + picker, matches ProfessionResolver's no-instance-state shape)
  </acceptance_criteria>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <done>EmployeeNames.java exists with a themed pool and a working random picker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EmployeeManager.bind — the core spawn method</name>
  <files>src/main/java/com/cxmxrgo/secondshift/employee/EmployeeManager.java</files>
  <read_first>
    - src/main/java/com/cxmxrgo/secondshift/content/block/SoulAltarBlock.java (validate-before-mutate discipline, lines 101-133)
    - src/main/java/com/cxmxrgo/secondshift/employee/EmployeeData.java (Plan 04-01 — exact field order)
    - src/main/java/com/cxmxrgo/secondshift/registry/ModAttachments.java (Plan 04-01 — EMPLOYEE holder)
    - .planning/phases/04-employee-attachment-spawn/04-RESEARCH.md (Pattern 2 "Spawn ordering", Pitfall A "position before addFreshEntity", Anti-Patterns list, Open Question 1 fallback)
  </read_first>
  <behavior>
    - Test 1: `bind` returns a non-null `Villager` whose `VillagerData.getProfession()` is one of `FARMER`, `LIBRARIAN`, `CLERIC` (D-03)
    - Test 2: the returned villager's `getVillagerXp() >= 1` (EMP-02)
    - Test 3: the returned villager's `getCustomName()` is non-null, styled `ChatFormatting.GREEN`, and `isCustomNameVisible()` is true (EMP-08, D-04)
    - Test 4: the returned villager `hasData(ModAttachments.EMPLOYEE.get())` is true, and `getData(...)` returns an `EmployeeData` whose `name()`/`profession()`/`tier()` match what was set on the entity (EMP-01)
    - Test 5: the returned villager is positioned at `altarPos.above()` (center of that block), not at world origin (Pitfall A regression)
  </behavior>
  <action>
    Create `employee/EmployeeManager.java`: a `public final class` with private no-arg constructor
    (static-utility shape per ProfessionResolver) and one public static method
    `public static Villager bind(ServerLevel level, BlockPos altarPos)`. Implementation, in this
    exact order (Pattern 2, non-negotiable):
    1. Pick `profession` randomly from a fixed `List.of(VillagerProfession.FARMER,
       VillagerProfession.LIBRARIAN, VillagerProfession.CLERIC)` via `level.getRandom()` (D-03).
    2. `Villager villager = EntityType.VILLAGER.create(level);` — throw `IllegalStateException` if
       null (should not happen, defensive per Pitfall A).
    3. Compute `BlockPos spawnPos = altarPos.above();` and call
       `villager.moveTo(spawnPos.getX() + 0.5D, spawnPos.getY(), spawnPos.getZ() + 0.5D, 0.0F, 0.0F);`
       BEFORE any other configuration (Pitfall A — position must be set before `addFreshEntity`,
       and doing it early avoids forgetting it).
    4. `villager.setVillagerData(villager.getVillagerData().setProfession(profession).setLevel(1));`
       — FIRST among data/offers/name calls (Pitfall 4: `setVillagerData` nulls `offers` on
       profession change, so profession must be set before `setOffers`).
    5. `villager.setVillagerXp(1);` (EMP-02 — non-negotiable, the only lever keeping
       `ResetProfession` from firing since this employee never claims a job-site POI).
    6. Materialize `MerchantOffers offers` via a private static helper
       `rollDefaultTier1Offers(Villager forEntity, VillagerProfession profession)` following the
       `<interfaces>` code example exactly (read `VillagerTrades.TRADES.get(profession)`, tier 1,
       call each `ItemListing#getOffer(forEntity, random)`, keep up to 2 non-null results, never
       call `villager.getOffers()` first per the Anti-Patterns list).
    7. `villager.setOffers(offers);` — LAST among data/offers calls (Pattern 2).
    8. `String name = EmployeeNames.pickRandom(level.getRandom());` then
       `villager.setCustomName(Component.literal(name).withStyle(ChatFormatting.GREEN));` and
       `villager.setCustomNameVisible(true);` (EMP-08, D-04 — do NOT touch `Villager`'s name-tag
       interaction handling anywhere in this class, per D-02).
    9. Resolve `ResourceLocation professionId = BuiltInRegistries.VILLAGER_PROFESSION.getKey(profession);`
       then `villager.setData(ModAttachments.EMPLOYEE.get(), new EmployeeData(1, name, professionId,
       1, offers));` (EMP-01) — this is the ONE legitimate `setData(EMPLOYEE, ...)` call site in the
       codebase; every other future read must use `hasData`.
    10. `level.addFreshEntity(villager);` — last call, triggers vanilla's own entity-add client sync
        (Pitfall B — do not also build a manual "entity exists" payload).
    11. `return villager;`
    Add a class Javadoc citing EMP-01/EMP-02/EMP-08/D-03/D-05 and the ordering-is-non-negotiable
    note from Pattern 2.
  </action>
  <acceptance_criteria>
    - All 5 behavior cases above hold, verified by the GameTests in Task 3
    - `setVillagerData(...)` is called strictly before `setOffers(...)` in the source (grep-checkable: the `setVillagerData` call appears earlier in the method body than `setOffers`)
    - `villager.getOffers()` is never called anywhere in `EmployeeManager.java` (only `setOffers` — grep `getOffers` returns zero matches in this file)
    - `addFreshEntity` is the last villager-mutating call before `return villager;`
  </acceptance_criteria>
  <verify>
    <automated>./gradlew runGameTestServer --tests "*EmployeeGameTests*"</automated>
  </verify>
  <done>EmployeeManager.bind exists, follows the exact spawn ordering, and every behavior case is proven by Task 3's GameTests.</done>
</task>

<task type="auto">
  <name>Task 3: EmployeeGameTests — EMP-01/02/08/09 automated proof</name>
  <files>src/main/java/com/cxmxrgo/secondshift/gametest/EmployeeGameTests.java</files>
  <read_first>
    - src/main/java/com/cxmxrgo/secondshift/gametest/HarvesterGameTests.java (authoring convention: template, helper.spawn, assertTrue/assertFalse, succeed)
    - src/main/java/com/cxmxrgo/secondshift/gametest/BindingAltarGameTests.java (helper.absolutePos usage against a fixed relative BlockPos)
    - src/main/java/com/cxmxrgo/secondshift/employee/EmployeeManager.java (the method under test — read after Task 2 completes)
  </read_first>
  <action>
    Create `gametest/EmployeeGameTests.java` under `@GameTestHolder(SecondShift.MODID)` +
    `@PrefixGameTestTemplate(false)`, template `"empty"`, mirroring `HarvesterGameTests`/
    `BindingAltarGameTests` idiom exactly (`helper.assertTrue`/`assertFalse`, `helper.succeed()`, no
    raw JUnit). Use a fixed relative `BlockPos ALTAR_POS = new BlockPos(4, 2, 4)` and
    `helper.absolutePos(ALTAR_POS)` when calling `EmployeeManager.bind(helper.getLevel(), ...)`.
    Write these methods:
    1. `bind_spawns_villager_with_employee_data` — call `bind`, assert
       `villager.hasData(ModAttachments.EMPLOYEE.get())` true, and the read-back `EmployeeData`'s
       `tier() == 1`, `name()` non-blank, `profession()` non-null (EMP-01).
    2. `bind_sets_villager_xp_at_least_one` — assert `villager.getVillagerXp() >= 1` (EMP-02).
    3. `bind_sets_green_always_visible_name` — assert `villager.getCustomName() != null`, its
       `Style#getColor()` equals `ChatFormatting.GREEN.getColor()` (or the style equivalent), and
       `villager.isCustomNameVisible()` is true (EMP-08).
    4. `bind_profession_is_one_of_three_fixed` — call `bind` several times (e.g. 20 iterations with
       distinct altar positions or reused position across calls) and assert every returned
       villager's `getVillagerData().getProfession()` is one of `FARMER`/`LIBRARIAN`/`CLERIC` (D-03).
    5. `wild_villager_unaffected_by_bind_in_same_world` — spawn a plain villager via
       `helper.spawn(EntityType.VILLAGER, SOME_OTHER_POS)` BEFORE calling `bind` elsewhere in the
       same test world; after `bind` runs, assert the wild villager's
       `hasData(ModAttachments.EMPLOYEE.get())` is false, `getCustomName()` is null, and
       `getVillagerXp() == 0` (EMP-09 — the regression check; this is the one test proving the bind
       path never touches an existing entity).
  </action>
  <acceptance_criteria>
    - `./gradlew runGameTestServer` reports all 5 new methods as passed, in addition to the existing 14 tests still passing (19 total)
    - `wild_villager_unaffected_by_bind_in_same_world` spawns its wild villager at a position distinct from where `bind` spawns its employee, so the two entities cannot be confused
  </acceptance_criteria>
  <verify>
    <automated>./gradlew runGameTestServer</automated>
  </verify>
  <done>All 5 GameTest methods exist and pass, and the full existing suite (19 tests total) remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A this plan | `EmployeeManager.bind` is called with server-derived arguments only (no direct client input parsing here — that validation is Plan 04-03's `ServerPayloadHandler`). This plan's threat surface is purely "does the wild-villager regression hold", covered below. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-04 | Tampering (unintended) | `EmployeeManager.bind` | mitigate | `bind` only ever constructs a brand-new `Villager` via `EntityType.VILLAGER.create(level)` — it never looks up or mutates an existing entity in the world. Verified by `wild_villager_unaffected_by_bind_in_same_world` (EMP-09). |

</threat_model>

<verification>
- `./gradlew runGameTestServer` — 19 tests total (14 existing + 5 new), all green.
- `./gradlew runServer` — client-class-leak gate stays clean (this plan's work is common-side).
</verification>

<success_criteria>
- `EmployeeManager.bind` spawns a correctly-configured, correctly-positioned, correctly-named employee villager with the exact ordering discipline RESEARCH.md requires.
- All 4 phase requirements (EMP-01, EMP-02, EMP-08, EMP-09) have automated GameTest proof for everything a single-tick GameTest can observe.
</success_criteria>

<output>
Create `.planning/phases/04-employee-attachment-spawn/04-02-SUMMARY.md` when done
</output>
