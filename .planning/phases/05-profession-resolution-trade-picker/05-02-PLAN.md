---
phase: 05-profession-resolution-trade-picker
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/cxmxrgo/secondshift/trade/TradePoolCache.java
  - src/main/java/com/cxmxrgo/secondshift/gametest/TradePoolCacheGameTests.java
autonomous: true
requirements: [PICK-02, PICK-05, PICK-08]

must_haves:
  truths:
    - "Rolling a profession's real tier-1 vanilla ItemListing pool produces concrete MerchantOffers with real items and prices, never a generic placeholder"
    - "A librarian's tier-1 roll includes exactly one freshly-rolled enchanted-book offer — never every enchantment enumerated separately"
    - "A cartographer's tier-1 roll (which can null out on TreasureMapForEmeralds) never throws and never leaks a null into the returned list"
  artifacts:
    - path: "src/main/java/com/cxmxrgo/secondshift/trade/TradePoolCache.java"
      provides: "rollTier1Candidates(ServerLevel, BlockPos, VillagerProfession) -> List<MerchantOffer>, a throwaway-villager-based, side-effect-aware tier-1 roll"
  key_links:
    - from: "src/main/java/com/cxmxrgo/secondshift/trade/TradePoolCache.java"
      to: "net.minecraft.world.entity.npc.VillagerTrades.TRADES"
      via: "direct read of the vanilla tier-1 ItemListing[] array for the given profession — no hardcoded per-profession trade data"
      pattern: "VillagerTrades\\.TRADES"
---

<objective>
Build the trade-pool materialization utility this phase's picker is built on: given a real
`VillagerProfession`, roll its actual vanilla tier-1 `ItemListing[]` pool into concrete
`MerchantOffer`s using a throwaway (never-added-to-level) villager, following RESEARCH.md Finding
1's exact null/side-effect handling — this is the LIGHT spike this phase's ROADMAP risk note calls
out, now resolved and encoded as production code plus a GameTest asserting no exception, no null
leak, and correct enchanted-book-count behavior for librarians.

Purpose: PICK-02 requires the picker to show the profession's REAL pool, not a generic list — this
class is the single place that pool is materialized, reused by every later plan in this phase
(menu wiring in 05-04, the network handler in 05-05).
Output: `TradePoolCache.rollTier1Candidates` and a GameTest suite proving it across the
null-risk (Cartographer) and enumeration-risk (Librarian) professions RESEARCH.md flagged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-profession-resolution-trade-picker/05-CONTEXT.md
@.planning/phases/05-profession-resolution-trade-picker/05-RESEARCH.md

<interfaces>
<!-- EmployeeManager's existing (soon-to-be-deleted in Plan 05-05) rollDefaultTier1Offers is the
     closest prior art in this codebase for reading VillagerTrades.TRADES — same registry access
     pattern, do not diverge from it gratuitously. -->
```java
Int2ObjectMap<VillagerTrades.ItemListing[]> byTier = VillagerTrades.TRADES.get(profession);
VillagerTrades.ItemListing[] tier1 = (byTier != null) ? byTier.get(1) : null;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TradePoolCache.rollTier1Candidates + GameTest suite</name>
  <files>src/main/java/com/cxmxrgo/secondshift/trade/TradePoolCache.java, src/main/java/com/cxmxrgo/secondshift/gametest/TradePoolCacheGameTests.java</files>
  <read_first>
    .planning/phases/05-profession-resolution-trade-picker/05-RESEARCH.md (Finding 1 in full — the exact side-effect and null-handling facts this class must encode)
    src/main/java/com/cxmxrgo/secondshift/employee/EmployeeManager.java (existing `rollDefaultTier1Offers` — the registry-access pattern to reuse, being deleted from this file in a later plan, not here)
    src/main/java/com/cxmxrgo/secondshift/gametest/EmployeeGameTests.java (GameTest idiom: `helper.assertTrue`/`assertFalse`/`succeed()`, `@GameTestHolder(SecondShift.MODID)`, `@PrefixGameTestTemplate(false)`, shared `secondshift:empty` template)
  </read_first>
  <behavior>
    - Test 1: `rollTier1Candidates` for `VillagerProfession.LIBRARIAN` returns a non-empty list
      whose result items include exactly one `Items.ENCHANTED_BOOK`-matching offer (PICK-05) — not
      more than one, not zero, across at least 10 independent rolls (fresh throwaway villager each
      roll) to rule out flakiness from `getOffer`'s internal randomness landing on a non-book
      listing by chance affecting the count of book listings specifically (the vanilla tier-1 array
      itself contains exactly one `EnchantBookForEmeralds` entry — the count-of-one assertion is
      about the ARRAY, not per-roll randomness, so a single roll suffices per profession but the
      test loops to also prove no exception across repeated rolls).
    - Test 2: `rollTier1Candidates` for `VillagerProfession.CARTOGRAPHER`, called from a throwaway
      moved to a real, generated chunk position (not `(0,0,0)`), never throws and returns a list
      with size `<= ` the real vanilla Cartographer tier-1 `ItemListing[]` length (a null
      `TreasureMapForEmeralds` result must be skipped, not crash, not padded with a placeholder).
    - Test 3: `rollTier1Candidates` for `VillagerProfession.FARMER` returns a non-empty list whose
      every `MerchantOffer` has a non-empty `getResult()` and a non-empty `getCostA()` (sanity check
      that real, concrete offers are produced, not empty stubs).
    - Test 4: the throwaway villager passed to `getOffer` is never added to the level — assert (via
      a level entity-count check before/after the roll) that `rollTier1Candidates` does not
      increase `helper.getLevel().getEntities(...)` count.
  </behavior>
  <action>
    Create `com.cxmxrgo.secondshift.trade.TradePoolCache` (final class, private constructor,
    mirrors `ProfessionResolver`'s static-utility shape). Add
    `public static List<MerchantOffer> rollTier1Candidates(ServerLevel level, BlockPos altarPos,
    VillagerProfession profession)`: construct a throwaway `Villager` via
    `EntityType.VILLAGER.create(level)` (guard against `null`, return `List.of()` if so — mirror
    `EmployeeManager.bind`'s existing null-guard style), `moveTo(altarPos.getX()+0.5,
    altarPos.getY(), altarPos.getZ()+0.5, 0.0F, 0.0F)` BEFORE rolling (RESEARCH Finding 1 — makes
    the Cartographer treasure-map search origin sensible), `setVillagerData(...setProfession(profession).setLevel(1))`
    so `getOffer`'s any profession-dependent logic sees the right profession. Do NOT call
    `level.addFreshEntity(throwaway)` — it must never join the level (Finding 1 Test 4). Look up
    `VillagerTrades.TRADES.get(profession)`'s tier-1 `ItemListing[]` exactly like
    `EmployeeManager.rollDefaultTier1Offers` does today; if the tier-1 array is null/absent, return
    `List.of()` (PICK-08 empty-pool case — no crash). Otherwise iterate every listing, call
    `listing.getOffer(throwaway, level.getRandom())` ONCE per listing, and skip (do not retry) any
    `null` result per Finding 1's explicit "do not loop retrying the same listing" instruction —
    collect non-null offers into a `List<MerchantOffer>` and return an immutable copy
    (`List.copyOf(...)`). This method must be called at most once per bind session by its caller
    (later plans) — do not add any caching/memoization inside this class itself, since "roll once"
    is a session-lifecycle concern owned by the BE (Plan 05-01's `candidatesRolled()`/
    `setCandidateOffers`), not this pure function.

    Write `TradePoolCacheGameTests` implementing the 4 behaviors above, matching
    `EmployeeGameTests`'s exact test-class shape (`@GameTestHolder(SecondShift.MODID)`,
    `@PrefixGameTestTemplate(false)`, `template = "empty"`, `helper.absolutePos(...)` for a fixed
    relative altar position, `helper.assertTrue`/`assertFalse`, `helper.succeed()`).
  </action>
  <acceptance_criteria>
    - `./gradlew runGameTestServer` passes all 4 new `TradePoolCacheGameTests` methods.
    - `TradePoolCache` has no non-private, non-static mutable state (pure function class).
    - `./gradlew compileJava` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <done>Acceptance criteria above are met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — `TradePoolCache` is a server-only, internally-invoked pure computation with no client input
and no persisted side effects of its own (it is the caller's responsibility, per Finding 1, to roll
at most once per session — enforced in Plan 05-04, not here).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-05-04 | Denial of Service (world-save bloat) | `TreasureMapForEmeralds#getOffer` orphaned map `SavedData` allocation | accept (this plan) / mitigate (Plan 05-04) | This plan's function is pure and stateless; the "roll once per session" discipline that actually prevents repeated map-data leaks is enforced by the BE-level `candidatesRolled()` guard added in Plan 05-01 and consumed in Plan 05-04 — flagged here for traceability, not re-mitigated in this plan |
</threat_model>

<verification>
- `./gradlew compileJava` after the task.
- `./gradlew runGameTestServer` (new `TradePoolCacheGameTests` + full existing suite).
</verification>

<success_criteria>
- `TradePoolCache.rollTier1Candidates` produces real, concrete offers for every tested profession,
  never crashes on a null-prone listing, never enumerates every enchantment for librarians, and
  never adds its throwaway villager to the level.
</success_criteria>

<output>
Create `.planning/phases/05-profession-resolution-trade-picker/05-02-SUMMARY.md` when done
</output>
