---
phase: 05-profession-resolution-trade-picker
plan: 07
type: execute
wave: 5
depends_on: ["05-03", "05-06"]
files_modified: []
autonomous: false
requirements: [ALTAR-02, GUI-03, PICK-05]

must_haves:
  truths:
    - "A human has visually confirmed the two altar sockets render distinctly with no overlap"
    - "A human has visually confirmed the Binding Altar screen shows correct name/profession/tier/trades"
    - "A human has visually confirmed a chosen enchanted-book trade displays correctly in the real vanilla trade screen"
  artifacts: []
  key_links: []
---

<objective>
Close out the phase's 3 manual-only verification items from 05-VALIDATION.md's "Manual-Only
Verifications" table — none of these are GameTest-observable (rendering and real-trade-screen
interaction), so this plan is the phase's single blocking checkpoint rather than scattering
non-blocking manual notes across the automated plans.

Purpose: every other requirement in this phase already has automated GameTest coverage (Plans
05-01 through 05-06); this plan is the deliberate, minimal exception for the 3 items the project's
own VALIDATION.md strategy flagged as structurally unautomatable.
Output: Explicit user confirmation that the phase's visual/interactive surface works as designed,
or a list of concrete issues to route back into a gap-closure plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-profession-resolution-trade-picker/05-VALIDATION.md
@.planning/phases/05-profession-resolution-trade-picker/05-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual verification of G-2 render, Binding Altar screen contents, and enchanted-book trade display</name>
  <files></files>
  <read_first>
    .planning/phases/05-profession-resolution-trade-picker/05-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <what-built>
    Plan 05-03's hovering/spinning job-item socket render, Plan 05-06's full Binding Altar screen
    (name field, candidate rows, Confirm), and the end-to-end bind flow from socketing both items
    through to a spawned employee with the chosen trades live in the real vanilla trade UI.
  </what-built>
  <action>
    Follow the manual verification steps below and report back with either "approved" or a
    concrete list of issues.
  </action>
  <how-to-verify>
    1. Run `./gradlew runClient`.
    2. Place a Soul Altar, get a Soul Block (harvest + craft per Phases 2-4) and a lectern.
    3. Right-click the lectern onto the altar, then the Soul Block (or the reverse order) — confirm
       both render simultaneously: the Soul Block embedded/glowing near the top, the lectern
       hovering higher and slowly spinning, with no visual clipping between the two.
    4. Confirm the Binding Altar screen opens automatically once both are socketed, shows
       "Librarian" as a bold gold heading, a pre-filled editable name field, and the real
       librarian tier-1 trade pool as rows with real items/prices — including exactly one
       enchanted-book row.
    5. Click 2 rows to select them (confirm the gold border + checkmark appears), click a 3rd row
       and confirm it flashes red briefly rather than replacing a selection.
    6. Edit the name field to something custom, click "Confirm Hire" — confirm both the Soul Block
       and the lectern are consumed, the altar's render returns to empty, and a named employee
       villager spawns nearby.
    7. Right-click the new employee to open the vanilla trade screen — confirm it shows exactly the
       2 chosen trades (not 2 random vanilla-fabricated ones), and the enchanted-book trade (if
       chosen) displays a real, specific enchantment and level, not a generic placeholder.
    8. Right-click the altar again (empty-handed) — confirm it shows the "already has an employee"
       themed message and does not open a menu.
  </how-to-verify>
  <verify>
    <automated>MISSING — rendering and real vanilla trade-screen interaction are not GameTest-observable; human confirmation via how-to-verify above is the only verification surface, per 05-VALIDATION.md's Manual-Only Verifications table</automated>
  </verify>
  <done>User has responded "approved" or issues have been captured for gap closure via `/gsd:plan-phase 5 --gaps`.</done>
  <resume-signal>Type "approved" or describe any issues found (they will be routed into a gap-closure plan via `/gsd:plan-phase 5 --gaps`)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this is a manual verification pass over already-shipped, already-threat-modeled code
(Plans 05-01 through 05-06 each carry their own `<threat_model>` block).

## STRIDE Threat Register

N/A — no new code is written in this plan.
</threat_model>

<verification>
Human confirmation via the checkpoint above. No automated command applies (rendering and real
trade-screen interaction are not GameTest-observable per 05-VALIDATION.md's Manual-Only
Verifications table).
</verification>

<success_criteria>
The user confirms all 3 manual-only items (G-2 render distinctness, GUI-03 screen contents, PICK-05
real trade-screen display) work as designed, or issues are captured for gap closure.
</success_criteria>

<output>
Create `.planning/phases/05-profession-resolution-trade-picker/05-07-SUMMARY.md` when done
</output>
